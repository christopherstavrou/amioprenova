# @christopherstavrou/* packages are published privately to GitHub Packages.
# Installs need NODE_AUTH_TOKEN set to a PAT with read:packages (local shell,
# CI secret, and the Cloudflare Pages env var).
@christopherstavrou:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
