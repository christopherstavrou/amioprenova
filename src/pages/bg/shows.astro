---
import Layout from '../../layouts/Layout.astro';
import { getUpcomingEvents, formatEventDate } from '../../lib/events';

const upcomingEvents = getUpcomingEvents();
---

<Layout
  title="Концерти - amioprenova"
  description="Намерете предстоящи концерти и изпълнения на amioprenova. Вземете билети и вижте дати, места и локации на концертите."
  lang="bg"
>
  <h1 class="text-4xl font-bold mb-8">Предстоящи концерти</h1>

  {upcomingEvents.length === 0 ? (
    <p class="text-gray-600 text-lg">
      Няма предстоящи концерти в момента. Проверете отново скоро или последвайте в социалните мрежи за обяви!
    </p>
  ) : (
    <div class="space-y-6">
      {upcomingEvents.map(event => (
        <div class="border border-gray-200 rounded-lg p-6 hover:border-gray-300 transition-colors">
          <div class="flex flex-col md:flex-row md:justify-between md:items-start gap-4">
            <div class="flex-grow">
              <h2 class="text-2xl font-bold mb-2">{event.title}</h2>
              <p class="text-gray-600 mb-1">
                <span class="font-medium">{formatEventDate(event.startDate, 'bg')}</span>
              </p>
              <p class="text-gray-600">
                {event.venue} · {event.city}, {event.country}
              </p>
            </div>
            <div class="flex gap-3">
              {event.ticketUrl && (
                <a
                  href={event.ticketUrl}
                  target="_blank"
                  rel="noopener noreferrer"
                  class="inline-block px-4 py-2 bg-gray-900 text-white text-sm rounded hover:bg-gray-700 transition-colors"
                >
                  Билети
                </a>
              )}
              {event.mapUrl && (
                <a
                  href={event.mapUrl}
                  target="_blank"
                  rel="noopener noreferrer"
                  class="inline-block px-4 py-2 border border-gray-300 text-gray-900 text-sm rounded hover:border-gray-400 transition-colors"
                >
                  Карта
                </a>
              )}
            </div>
          </div>
        </div>
      ))}
    </div>
  )}
</Layout>
